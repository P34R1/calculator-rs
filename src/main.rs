#![allow(dead_code, unused_variables, unused_imports)]

mod calculator;
use crate::calculator::calculate;

fn main() {
    println!("Hello, world!");

    // Get equation

    // calculate(equation)
}
